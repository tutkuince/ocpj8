package com.oracle.javacert.professional.chapter07._01introducingthreads.definition;

public class Notes {
	/*
	 * We can create Thread with 2 ways;
	 * 1-) We can extends Thread class and override run() method
	 * 2-) We can implements Runnable interface and implement run() method
	 * 3-) We can use anonymous class in thread constructor
	 * 4-) We can use ExecutorService
	 * */
}
